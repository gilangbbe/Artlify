# Roadmap — Artlify

> Living document. Update at least weekly. Each item lists **Priority** (P0=blocker, P1=must, P2=should, P3=nice), **Status**, **Dependencies**, and **Estimated effort** in engineer-days (ed). Effort is a coarse t-shirt-sized estimate, not a deadline.

---

## Milestones (v1)

Each milestone produces a **runnable, demoable build**. We do not start the next until the previous is green.

| ID | Milestone                                  | Definition of done                                                                                  | Depends on |
| -- | ------------------------------------------ | --------------------------------------------------------------------------------------------------- | ---------- |
| M0 | Project skeleton + camera passthrough      | Xcode project with 6 module folders. App opens iPhone via Continuity Camera, displays raw feed in an MTKView at 30 FPS. End-to-end camera latency measured and logged. | —          |
| M1 | Diffusion benchmark + first stylized still | SD Turbo CoreML model loads, runs img2img on a single captured frame, displays result. FPS, latency, peak RAM logged. | M0         |
| M2 | Vision integration                         | Person segmentation mask + body pose overlay rendered live at 30 FPS. Person count surfaced.        | M0         |
| M3 | Live diffusion loop + temporal blend       | Diffusion runs continuously off the latest frame. Renderer blends latest two AI outputs at 60 FPS. Camera silhouette composited on top. Drop-stale-frame policy verified under load. | M1, M2     |
| M4 | PromptKit + UI                             | SwiftUI panel: prompt text field, style preset picker, eco/quality toggle, FPS HUD, fullscreen toggle. Pose/motion modulate prompt per §8 of `ProjectDocument.md`. | M3         |
| M5 | Hardening + demo polish                    | Reconnection handling, graceful diffusion-stall fallback, Models.lock + first-launch download UI, 30-minute thermal soak test passes. Ready to demo. | M4         |

---

## Backlog

### P0 — Blockers / unknowns to resolve before committing further

| Item                                                                                  | Status        | Depends on | Effort |
| ------------------------------------------------------------------------------------- | ------------- | ---------- | ------ |
| Confirm `ml-stable-diffusion` Swift package minimum macOS version                     | resolved (1.1.1, macOS 13.1+) | — | — |
| Verify Continuity Camera works headlessly (no user gesture each launch)               | resolved (notification-driven auto-switch + manual Reconnect button) | M0 | — |
| Convert SD Turbo to CoreML and place it in `~/Library/Application Support/Artlify/Models/sd-turbo/` | resolved (user did the conversion, model loads) | M1 code | — |
| Benchmark SD Turbo on M5 base: 512×512, fp16, 2 steps → measure FPS, latency, RAM     | **resolved — 512/ANE = 997 ms, 384/ANE = 660 ms (default in M4); below the ≥3 FPS target** (see ProjectDocument §13 #1) | M1 | — |
| Confirm zero-copy path from CoreML output (`MLShapedArray`) → `MTLTexture`            | not started   | M3         | 0.5 ed |

### P1 — Required for v1

| Item                                                                                  | Status      | Depends on | Effort |
| ------------------------------------------------------------------------------------- | ----------- | ---------- | ------ |
| **Diffusion-perf A/Bs given measured 1 FPS** — try 384×384, `.cpuAndGPU`, split UNet/VAE compute units | done — 384/ANE chosen as default (660 ms vs 512/ANE's 997 ms); pickers stay in HUD for manual override | M3         | —      |
| `CaptureKit` module: AVCaptureSession wrapper, `AsyncStream<CVPixelBuffer>`           | done        | M0         | —      |
| `RenderKit` module: MTKView, texture pool, basic blit pipeline                        | done        | M0         | —      |
| `VisionKit` module: person seg @ 30 Hz + pose @ 15 Hz, EMA mask smoothing             | done (`.balanced` seg + body pose at 15 Hz cap, runs in single VNImageRequestHandler) | M2         | —      |
| `DiffusionKit` module: model load, img2img call, error handling, fp16 path            | done        | M1         | —      |
| FrameRouter actor (latest-frame-wins, drop-stale)                                     | done (folded into `AsyncStream.bufferingNewest(1)` in CaptureKit) | M0 | — |
| Temporal blend shader (lerp between two AI textures by timestamp)                     | done (composite_fragment, EMA-tuned cycle length) | M3 | — |
| Silhouette composite shader (camera mask over AI texture)                             | done (folded into composite_fragment, mask_enabled uniform) | M3 | — |
| `PromptKit`: rule-based modifier mapping + denoising-strength controller              | done (StylePreset library + PromptComposer with pose + motion modifiers) | M4 | — |
| SwiftUI shell: prompt input, presets, HUD, fullscreen                                 | done (preset chips, extra-prompt field, fullscreen button, effective-prompt readout, modifier toggles) | M4 | — |
| First-launch model downloader with SHA256 verification + progress UI                  | not started | M5         | 1.5 ed |
| Diffusion-stall fallback (>2 s no output → camera + post-FX only)                     | not started | M5         | 0.5 ed |
| Continuity Camera reconnect handling                                                  | done        | M5         | —      |
| FPS / latency / RAM HUD (DEBUG builds always; user-toggleable in release)             | partial (per-subsystem ms/Hz lines in HUD; no RAM yet) | M3 | 0.25 ed |
| 30-minute thermal soak test + eco-mode tuning                                         | not started | M5         | 1 ed   |

### P2 — Should-have polish

| Item                                                                          | Status      | Depends on | Effort |
| ----------------------------------------------------------------------------- | ----------- | ---------- | ------ |
| Bloom + grain + palette post-FX shaders                                       | not started | M3         | 1 ed   |
| Pose-delta warp between diffusion frames (cheap optical-flow approximation)   | not started | M3         | 2 ed   |
| Style preset library (5–8 hand-tuned prompts)                                 | done (8 presets in `PromptKit/StylePreset.swift`) | M4 | — |
| Hot-reload of `.metal` shader files in DEBUG                                  | not started | M0         | 0.5 ed |
| Unit tests for `PromptKit` mapping rules                                      | not started | M4         | 0.5 ed |
| Mock implementations of each module behind protocols (for offline UI dev)    | not started | M0         | 1 ed   |

### P3 — Nice-to-have

| Item                                                       | Status      |
| ---------------------------------------------------------- | ----------- |
| MetalFX upscaling pass (M5 supports it)                    | not started |
| Per-style LUT color grading                                | not started |
| Save current frame to disk (PNG)                           | not started |
| Session video recording (ScreenCaptureKit)                 | not started |
| **Karaoke stage 2** — MusicKit catalog search + `ApplicationMusicPlayer` playback, LRCLIB synced-lyric fetch (`lrclib.net/api/get`), ScreenCaptureKit system-audio capture swapped into `AudioReactor` (replaces mic). Overlay unchanged thanks to stage 1's passive store. Adds `NSAppleMusicUsageDescription` + Screen Recording permission. | **done** — shipped 2026-05-18 → 2026-05-19 across four slices: UI sheet, LRCLIB fetch, audio-file player, MusicKit (and SCK ultimately removed in favour of mic-on-speakers). |

---

## In Progress

| Item                                                                            | Status        | Notes                                                                              |
| ------------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------- |
| **Branch `particles` — swarm + trails + audio + body-bursting shockwaves + neg-camera flash boxes + ASCII-dither overlay (custom hue) + blob-tracking bounding-box overlay** | build green, untested on hardware | Dark bg, mask-attractive force, curl-noise drift, alpha gated by silhouette. Trail accumulator (ping-pong + decay) leaves fluid wakes. AudioReactor (AVAudioEngine + vDSP FFT, low/mid/high + level + pan + transient) modulates flow, glow, hue, fires a stereo-panned shockwave from `bodyCenter` with relaxed mask gate so particles burst OUT through the silhouette. Random negative-camera windows (`NegativeBoxes.metal`) flash around body joints at ~9 Hz. ASCII overlay (`Ascii.metal` + glyph atlas) re-renders camera-through-silhouette as monospaced glyphs sparse→dense by luminance, single hue slider drives `colorLow`/`colorHigh`, audio-triggered ring expands from body anchor on each transient. Blob bounding-box overlay (SwiftUI Canvas, `AppShell/BlobBoxes.swift`) tracks each pose joint as a smoothed-position blob, draws an outlined corner-ticked box around it, randomly flashes subsets of boxes on the same ~9 Hz timer, draws white connective "strings" between currently-lit boxes for a tracker-diagram look. New mic entitlement + Info.plist key. |
| **M5 — Hardening + demo polish** | not started, unblocked by M4 ship | First-launch model downloader, diffusion-stall fade-out, 30-min thermal soak, minimal-HUD demo mode. |

---

## Completed

| Item                                                                                       | Date       |
| ------------------------------------------------------------------------------------------ | ---------- |
| **Branch `particles` — scrapped hand-frame, pivoted to open-hand → negative-camera flash** — on-hardware intuition was that the bimanual L-shape "filmmaker frame" shipped earlier the same day is too brittle (thumb + indexMCP confidences collapse under installation lighting, and visitors don't reflexively form the gesture). Replaced with a single-hand open-palm trigger that bursts a full-screen inverted-camera flash. Deleted `VisionKit/HandFrame.swift` + `RenderKit/HandFrame.metal`. New `VisionKit/HandOpen.swift` (~140 lines, `nonisolated` throughout): `HandObservation { chirality, wrist, tips:[4], mcps:[4], confidence }` (4 = index/middle/ring/little); `HandOpenInfo { openness, center, chirality }`; `HandOpenDetector.detect(_:) -> HandOpenInfo?` picks the most-open hand crossing threshold. **Extension ratio** per finger = `‖tip − wrist‖ / ‖mcp − wrist‖` (scale-, distance- and orientation-invariant — wrist→MCP baseline cancels all three out); closed fist ≈ 1.0, open palm ≈ 1.8–2.4; `threshold = 1.55`, `minConfidence = 0.35`, requires ≥ 3 of 4 fingers confidently tracked. New `RenderKit/NegativeFlash.metal` fullscreen fragment: samples `cam` at `in.uv`, outputs premultiplied `(1 − cam.rgb) × intensity` with `α = intensity`, alpha-blended over the drawable, discards when `intensity ≤ 0.001` (cheap: one sample + one subtract). `VisionProcessor.makeObservation(from:)` now pulls wrist + 4 tips + 4 MCPs at landmark confidence ≥ 0.3 and feeds `HandOpenDetector.detect(top-two-by-confidence)`; result lands on `VisionFrame.handOpen`. `CameraMetalRenderer`: pipeline renamed `negFlashPipeline`, new `negFlashEnabled` flag + `setNegFlash(intensity:)` (clamps + caches a 4-Float padded uniform), `encodeNegFlash` drawn as Step 5. `ContentView`: `negFlashEnabled` + `negFlashIntensity` + `handWasOpen` (rising-edge state) + `negFlashLastTrigger` (rate-limit). In `onChange(of: vision.passCount)`: when enabled and `!handWasOpen && isOpen` and ≥ 0.55 s since the last kick, set `negFlashIntensity = 1.0`. Decay lives in the existing 60 Hz `karaokeTimer.onReceive` *before* the karaoke guard (so the flash fades even with the karaoke overlay off): multiplier 0.92/frame → ~0.4 s to 1 %, snapped to 0 below 0.002, pushed via `setNegFlash(intensity:)` every tick. HUD toggle re-labelled `neg flash`; its `onChange` flips both `vision.setHandPoseEnabled` and `renderer.negFlashEnabled` together. Rising-edge + rate-limit + multiplicative decay turns the gesture into a discrete strobe-pulse *event* rather than a continuous overlay. Build green with `CODE_SIGNING_ALLOWED=NO`. | 2026-05-19 |
| **Branch `particles` — CMIO queue-full fix + karaoke layer-3 toggle + hand-frame ("filmmaker frame") gesture** — three changes in one cycle. (1) **Killed `CMIO_DAL_CMIOExtension_Stream.mm:3547:ReceivedSampleBuffer N queue full`** by capping the Continuity camera at 30 fps device-side (`CameraCapture` now `lockForConfiguration`s + sets `activeVideoMin/MaxFrameDuration = (1,30)`, clamped to the active format's `videoSupportedFrameRateRanges` so non-Continuity devices stay safe) and flushing `CVMetalTextureCache` (`CVMetalTextureCacheFlush(cache, 0)`) at the end of both `submit(_:)` and `submitMask(_:)` so source `IOSurface`s return to the camera daemon's pool before it runs out. `alwaysDiscardsLateVideoFrames` only drops frames *after* they enter our process; the queue that was overflowing lives upstream of that. (2) **Layer-3 lyric toggle** for `KaraokeOverlay`: new `var showCurrentLine: Bool = true` gates the loud chromatic + chaos current-line `drawCurrentLine` call; layers 1 (world fragments), 2 (prev/next satellites), 4a (slice tear), 4b (bloom) keep running. HUD toggle `layer 3 lyric` next to the new `hand frame` toggle. Shipped because hardware testing showed the giant centred lyric can overpower the visual on vocal-heavy songs. (3) **"Filmmaker frame" hand-pose gesture** — bimanual two-hand L-shape (thumb + index of each hand) → oriented rectangle → INSIDE shows live camera at original screen uv (the gesture is a "lens" onto reality, so the framed area reveals the unprocessed camera behind the stylised scene), OUTSIDE keeps the full pipeline, four amber corner brackets mark the rectangle. New `VisionKit/HandFrame.swift` (~180 lines, `nonisolated` throughout): `HandObservation` (chirality + wrist + thumbTip + indexTip + indexMCP), `HandFrameInfo` (center + halfSize + angle + corners + quality), `HandFrameDetector.detect` (per-hand: thumb-wrist-index angle ∈ [50°, 130°] + thumb-index span ≥ 4%; pair: indexMCP↔indexMCP diagonal ≥ 15%; rectangle is a square whose two opposite corners coincide with the indexMCP anchors, so halfW = halfH = D/(2√2) and rotation = atan2(diagonal) − π/4). Quality blends 90°-proximity (60%) + landmark confidence (40%). `VisionKit/VisionProcessor.swift` extended with `VNDetectHumanHandPoseRequest` (maximumHandCount=2), opt-in via `handPoseEnabled` actor-isolated flag (off by default so existing modes pay zero extra Vision cost), conditional `try handler.perform([…])` append, top-two-by-confidence `HandObservation` extraction at landmark-confidence ≥ 0.3, hand-off to `HandFrameDetector.detect`. Result lands on `VisionFrame.handFrame: HandFrameInfo?`. New `RenderKit/HandFrame.metal` fullscreen fragment pass: rotates `(uv − centre)` into rect-local space, samples `cam` at unmodified screen uv inside the rectangle, draws four L-shaped corner brackets in the border band, `discard_fragment()` elsewhere; output premultiplied amber `(1.0, 0.78, 0.18)` × intensity for brackets, opaque camera inside, alpha-blended over the drawable. `RenderKit/CameraMetalRenderer` gained `handFramePipeline` (passthrough_vertex + handframe_fragment + premultiplied alpha blend), `handFrameEnabled` flag, `handFrameBorderHalfWidth: 0.006` + `handFrameBracketFraction: 0.22` knobs, `setHandFrame(_:intensity:)` (Vision→screen coord flip: cy = 1 − vy, angle negated), `encodeHandFrame` as Step 5 of `draw(in:)`. `AppShell/VisionSession.setHandPoseEnabled(_:)` forwards to the actor. `ContentView`: `@State handFrameEnabled` + `@State handFrameSmoothQuality` (EMA α=0.25 so a single Vision dropout can't collapse the rectangle), HUD row with `hand frame` + `layer 3 lyric` toggles. Hand-frame toggle's `onChange` flips both `vision.setHandPoseEnabled` and `renderer.handFrameEnabled` (enabling either alone is a no-op). Build green with `CODE_SIGNING_ALLOWED=NO`. | 2026-05-19 |
| **Branch `particles` — karaoke phase 2 cycle complete: SCK removal + no-sound fix + Apple Music (MusicKit) integration** — three changes shipped together that close phase 2 for the installation context. (1) **Removed `SystemAudioCapture.swift` entirely** + dropped the `.systemAudio` `AudioReactor.InputSource` case. On-hardware testing confirmed the mic pointed at the installation speakers gives a clean reactive signal (and is more honest — same air the audience hears). Subtracts: TCC prompt UX, SCStream availability check, "Dropping frame" workaround, macOS-13 `@available` guard. HUD source picker shrinks from 3 → 2 icons. (2) **Fixed silent file playback** in `AudioFilePlayer`: (a) touch `engine.mainMixerNode` + `outputVolume = 1.0` in `init()` so the mixer→output edge auto-connects at hardware format *before* the player attaches (previously the lazy auto-connect landed at the player's `processingFormat` and silently produced no audio on some configs); (b) `stop()` now only stops the player node — never `engine.stop()` — because tearing down the engine between plays severs mixer→output on macOS 26.x and silences subsequent plays. (3) **Wired Apple Music** as the primary catalog source for installation visitors. New `AppShell/MusicKitClient.swift` (~85 lines): `MusicKitError: LocalizedError`, `ensureAuthorized()` (idempotent `MusicAuthorization.request()`), `searchSongs(query:limit:)` wrapping `MusicCatalogSearchRequest(term:types:[Song.self])` (capped at server max 25). New `AppShell/MusicKitPlayer.swift` (~150 lines): `@Observable @MainActor final class` wrapping `ApplicationMusicPlayer.shared`. Published `currentTime / duration / isPlaying / isActive / currentSongTitle / currentSongArtist / lastError`. `play(song:)` queues + awaits `try await player.play()`, `pause / resume / stop / seek(to:)` delegate directly, internal 10 Hz `Task` poll mirrors `player.playbackTime` + `state.playbackStatus` to published props (no explicit `deinit` — `[weak self]` cancels naturally). `MusicSearchSheet` extended: `MusicSearchResult` now carries `kind: MusicResultKind` (`.demo / .lrclib / .appleMusic(Song)` — the Song value is held so `play(song:)` needs no re-fetch); new `.appleMusic` case in `MusicSearchSource` with pink `APPLE MUSIC` badge; `runSearch()` branches to `MusicKitClient.searchSongs`; per-source placeholder/footer/empty-state copy; default source flipped from `.demo` → `.appleMusic`. `ContentView`: `@State musicKit = MusicKitPlayer()`; sheet `onSelect` branches on `result.kind`, for `.appleMusic` it stops the file player, ensures mic+running for analysis (`ApplicationMusicPlayer` runs in an XPC daemon and doesn't expose buffers — mic-on-speakers is the path), calls `await musicKit.play(song:)`, then concurrently fires `LRCLibClient.search(track:artist:)` for synced lyrics (best-effort — song plays without lyrics if LRCLIB has nothing). Karaoke 60 Hz timer source-priority ladder: `musicKit.isActive` > `audioFile.fileURL != nil` > wall-clock; MusicKit branch also mirrors `musicKit.isPlaying` back onto `karaokePlaying` so HUD tracks OS-side pauses (phone call, AirPods removal). Transport (play/pause/stop/scrub/timecode) all branch on `musicKit.isActive` and forward to `musicKit.{resume,pause,stop,seek}`; disable predicates relaxed from `karaoke.track == nil` to `karaoke.track == nil && !musicKit.isActive` so controls stay live for songs without LRC coverage. Both transport paths call `.stop()` on the other player to prevent two audio sources fighting. Infra: added `com.apple.developer.musickit` to `Artlify.entitlements`; added `INFOPLIST_KEY_NSAppleMusicUsageDescription = "Artlify lets visitors search Apple Music and play their chosen song as the karaoke source."` to both Debug + Release `project.pbxproj` configs. Code builds clean (`CODE_SIGNING_ALLOWED=NO` → BUILD SUCCEEDED). Deployment prerequisites (separate from code): paid Apple Developer Program membership, installation Mac registered as dev device, `com.biru.Artlify` App ID provisioned with MusicKit capability in the portal, signed-in Apple Music subscription on the Mac. Three search backends (Demo / LRCLIB / Apple Music) now coexist behind one row; phase 2 of karaoke is functionally complete. | 2026-05-19 |
| **Branch `particles` — karaoke phase 2 (offline slice): local audio file playback + sample-accurate karaoke time lock + SCK warning fix** — new `AudioKit/AudioFilePlayer.swift`: `@Observable` `AVAudioEngine` + `AVAudioPlayerNode` + `AVAudioFile` wrapper exposing `load(url:) / play / pause / stop / seek(to:)` plus published `fileURL / fileName / duration / currentTime / isPlaying / lastError`. `load(url:)` disconnects + reconnects the player node to mainMixer at the new file's `processingFormat` (handles SR/channel changes between tracks) and installs a tap on bus 0. `play()` uses `scheduleSegment(file, startingFrame: seekOffsetSamples, frameCount: remaining)` so resume-after-seek is exact; completion handler dispatches to main, flips `isPlaying`, fires `onFinished`. Tap callback (a) forwards `AVAudioPCMBuffer` to `onAudioBuffer?` for the reactor, (b) converts `player.lastRenderTime → playerTime(forNodeTime:) → sampleTime` and adds `seekOffsetSamples` for absolute file position — the canonical CoreAudio "where am I" pattern, survives pause/resume cleanly. `@MainActor static func runOpenPanel()` returns user-picked URL via `NSOpenPanel` configured for `.mp3 / .mpeg4Audio / .wav / .aiff / .audio` UTTypes. `AudioReactor` grew a third `InputSource.audioFile` case (reactor owns no engine for this case — `startAudioFile()` just flips `isRunning`) + `public func ingest(buffer:)` that no-ops unless `source == .audioFile && isRunning` (guards against stale taps after source switch). `ContentView`: `@State audioFile = AudioFilePlayer()`, `.onAppear` wires `audioFile.onAudioBuffer = { audio.ingest(buffer: $0) }` (always-on, safe) + `onFinished = { karaokePlaying = false }`. Karaoke 60 Hz timer now branches: if `audioFile.fileURL != nil` then `karaoke.currentTime = audioFile.currentTime` (sample-accurate from the player node, drift-immune); else fall back to wall-clock integrator. New `file` (folder SF Symbol) button in `karaokeRow` opens the panel, loads file, switches reactor to `.audioFile`, starts engine + player, falls back to `karaoke.loadSample()` if no LRC is loaded yet so any file gets the karaoke shell + reactive visuals against it. Play/pause button mirrors onto `audioFile.play()/pause()` when a file is loaded; stop calls `audioFile.stop()`; scrub slider's `set:` calls `audioFile.seek(to:)` so audio follows the lyric scrub. HUD source picker grew a third tag (`music.note` for `.audioFile`, width 80 → 120 pt). **Also fixed**: SCK `_SCStream_RemoteVideoQueueOperationHandlerWithError:1459 stream output NOT found. Dropping frame` log spam by registering a no-op `.screen` output alongside `.audio` in `SystemAudioCapture` — `didOutputSampleBuffer`'s existing type guard drops the screen frames silently. Build green. Karaoke now functionally complete as an offline experience (file + LRC = locked playhead, no Internet, no TCC). | 2026-05-18 |
| **Branch `particles` — karaoke phase 2 (audio slice): ScreenCaptureKit system-audio source** — new `AudioKit/SystemAudioCapture.swift`: `SCStream`-wrapping class (`SCStreamOutput` + `SCStreamDelegate`) that picks `SCShareableContent.displays.first`, configures `capturesAudio = true / 48 kHz / stereo / excludesCurrentProcessAudio` with a deliberately minimal 2×2 1 FPS video config (SCStream requires video dimensions but we never subscribe to `.screen` output so the compositor skips that work), adds only `.audio` to `addStreamOutput`, converts incoming `CMSampleBuffer`s to `AVAudioPCMBuffer` via the embedded `AudioStreamBasicDescription` + `CMSampleBufferCopyPCMDataIntoAudioBufferList` (SCStream gives Float32 non-interleaved on Apple Silicon — exact match for the existing FFT path). `SystemAudioError: LocalizedError` for the rare unhappy paths. `AudioReactor` refactor: new `InputSource { microphone, systemAudio }` enum + `source` published property + `switchSource(_:)` that tears down current + restarts new, split monolithic start/stop into `startMicrophone` / `startSystemAudio` / matching stops, lazy `systemCapture` (only pay SCK init when actually used), shared `handleTap(buffer:)` consumed by both sources so FFT/band/pan/transient pipeline is identical, auto-fall-back to mic on `didStopWithError` (catches permission-revoked-mid-session). HUD `audioRow` gained an icon-only segmented `Picker` (mic ↔ speaker, 80-pt wide, `.help` tooltip explaining TCC) bound to `audio.source` / `audio.switchSource`. First flip triggers the "Screen & System Audio Recording" TCC prompt; subsequent launches silent. No new entitlements — TCC is the only gate. Every reactive layer (chromatic split width, head-blob string sag, particle flow strength, lyric brightness, transient explosion jolts) now pulses on whatever the Mac is playing in any app. | 2026-05-18 |
| **Branch `particles` — karaoke phase 2 (network slice): LRCLIB live lyrics fetch** — new `AppShell/LRCLibClient.swift`: `LRCLibTrack` (Decodable mirroring upstream JSON exactly so no `CodingKeys`), `LRCLibError: LocalizedError` (narrow enum: badURL/transport/http/decode/noResults/noSyncedLyrics with human descriptions), `enum LRCLibClient` static-async-throws methods (`search(query:)`, `search(track:artist:)`, `getByID(_:)`) hitting `https://lrclib.net/api/{search,get}` with required `User-Agent` header + 10 s timeout. `MusicSearchSheet` rewritten: new `MusicSearchSource` enum with `.segmented` Picker (Demo/LRCLIB), results now `@State` driven by `runSearch()` on `@MainActor` instead of a synchronous computed property, 350 ms-debounced `.onChange(of: query)` task that cancels in-flight on every keystroke (plus `Task.isCancelled` guards + `CancellationError` catch — full three-layer race protection), inline `ProgressView` in the search field during fetch, dedicated error state with `Retry` button reading `LRCLibError.errorDescription`, source badge now colour-coded (orange `DEMO CATALOG` vs green `LIVE — LRCLIB`), footer hint + empty-state copy adapt to source. `mapLRCLib(_:)` drops hits without `syncedLyrics` because the overlay needs time stamps. Selection path unchanged: `result.lrc` still flows through `LRCParser.parse` into `karaoke.track` — both sources reuse the unified `MusicSearchResult` shape (now proven across two mappings). Uses existing `com.apple.security.network.client` entitlement; no new capabilities. | 2026-05-18 |
| **Branch `particles` — karaoke phase 2 (UI slice): music search sheet + expanded HUD control panel** — new `AppShell/MusicSearchSheet.swift`: `MusicSearchResult` (id/title/artist/duration/artwork SF symbol/lrc — field shape matches MusicKit `Song` for a 1-for-1 stage-2 swap), `MockMusicCatalog` (5 hardcoded tracks all backed by `LRCParser.sample`, substring filter across title+artist), `MusicSearchSheet` view (header with bold orange `DEMO CATALOG` badge to prevent "where's my song" confusion, plain `TextField` with clear button, `LazyVStack` of result rows with artwork tile + title/artist + duration + play/check icon, 120 ms highlight-then-dismiss on pick, empty state, footer hint about stage-2 plumbing, `.regularMaterial` 460–520 × 420–520 frame). HUD `karaokeRow` gained a prominent `search` button (borderedProminent) that opens the sheet, a `stop` eject button that calls `karaoke.clear()`, and a "now playing" pill (green/grey liveness dot + track title) after the timecode. Sheet's `onSelect` parses `result.lrc` into `karaoke.track`, resets time, sets `currentTrackTitle`, enables overlay + starts playback. **Pure UI slice — no MusicKit, no LRCLIB, no network**; stage-2 swap is replacing `MockMusicCatalog.search(_:)` with `MusicCatalogSearchRequest` and `result.lrc` with an LRCLIB GET. | 2026-05-18 |
| **Branch `particles` — karaoke phase 2 (first slice): head-tethered lyric blob** — new `HeadLyricBlob` view in `AppShell/Karaoke.swift`, toggle-able, anchors a wobbly organic blob beside the person's head (read off the `nose` joint with aspect-fill projection matching `PoseOverlay`, side picked from screen-half so it stays on-frame). String from head to blob is a sagging chromatic-split quad-bezier (sag deepens with `low`, splits with `high + transient`). 48-segment blob path with four-sine radial wobble for organic breathing, dark plate fill + RGB-offset stroke triplet + soft inner halo. Inner lyric uses per-glyph chromatic split at 15 pt, lit in lock-step with the main overlay via the shared `store.lineProgress`. Independent toggle in HUD next to `karaoke` — both can run together (focal bottom-line + head annotation). **Phase-2 network slice still pending** (MusicKit search + ApplicationMusicPlayer playback + LRCLIB fetch + ScreenCaptureKit system audio) — those land separately. | 2026-05-18 |
| **Branch `particles` — karaoke overlay → cinematic chaotic visualiser** — same-day rewrite of `KaraokeOverlay` after stage 1 felt too tame. Five additive (`BlendMode.plusLighter`) layers: (1) **world ghost fragments** — 7 huge chromatically-split word chunks scattered with per-line-seeded RNG, breathing with the low band; (2) **radial bloom** behind the line, radius `140 + 220·level + 280·low`, so the lyrics emit light into the swarm; (3) **prev/next satellites** parallax-drifted off-axis instead of stacked above/below; (4) **transient slice-tear** VHS scan glitch at noise-displaced y on each attack; (5) **current line** with real RGB chromatic aberration (three coloured passes offset by `splitX = 2 + 14·level + 28·transient`), per-glyph pseudoNoise micro-rotation + transient explosion jolts, font-size breathing, karaoke wipe now reads as saturation lighting up rather than brightness on/off. Global camera-shake envelope (`shake = max(shake·0.90, transient)`) translates the whole canvas. New audio params plumbed: `audioLow`, `audioTransient`. | 2026-05-18 |
| **Branch `particles` — karaoke synced-lyrics overlay (stage 1)** — new `AppShell/Karaoke.swift`: `LyricsLine` + `LyricsTrack`, `LRCParser` (regex for `[mm:ss(.xxx)?]` timestamps incl. multi-stamp choruses + `(ti|ar|al)` metadata tags, hardcoded `sample` LRC), `KaraokeStore` (@Observable, passive — holds `track` + `currentTime`, computes `currentLineIndex` and `lineProgress`), `KaraokeOverlay` (SwiftUI `Canvas`, own 60 Hz `Timer.publish` repaint, draws prev/current/next stack at 78% height, per-character resolve so each glyph carries its own colour-by-highlight-wipe and waveform y-offset = two summed sines driven by `audio.latest.level/mid/high`). Wired in ContentView: state (`karaoke`, `karaokeEnabled`, `karaokePlaying`, `karaokeLastTick`, dedicated 60 Hz `karaokeTimer`), `.onReceive(karaokeTimer)` advances `currentTime` by wall-clock dt clamped to track duration, HUD row with toggle / sample-loader / play-pause / scrub slider / `m:ss / m:ss` timecode. **Stage 1 deliberately omits**: MusicKit search/playback, LRCLIB fetch, ScreenCaptureKit system-audio tap — those land in stage 2 with zero overlay changes thanks to the passive store + plain-Double audio bindings. | 2026-05-18 |
| **Branch `particles` — blob bounding-box overlay (replaces rejected tracery overlay)** — user clarified the brief: blob tracking should produce flashing **bounding boxes** on each tracked body region with **strings connecting them**, on the same flicker idiom as the negative-camera boxes; tracery splines were too organic. Deleted `AppShell/BlobTracery.swift`. New `AppShell/BlobBoxes.swift`: `BlobBox` (id, smoothed center, halfSize, hash hue, `flashUntil`, lastSeen), `BlobBoxStore` (@Observable, EMA position smoothing, prune-on-stale, `tickFlash` rolls per-box flash-up with `flashProbability` 0.30 default and `flashDuration` 0.16 s), `BlobBoxesOverlay` (SwiftUI `Canvas`, 60 Hz `Timer.publish` repaint). Per lit box: glow underlay + crisp outlined rect + corner tick brackets + centre dot, triangular alpha envelope inside the flash window. Connective web: every-pair white string with subtle glow drawn before the boxes. Driver in ContentView: `updateBlobs()` from existing `.onChange(of: vision.passCount)` for positions; `blobs.tickFlash()` added to the existing 9 Hz `boxTimer` so blob and negative-camera flickers cohere. HUD: `blobs` toggle, `strings` toggle, `flash` probability slider (0.05–0.8), `intensity` slider (0.2–1.5). | 2026-05-13 |
| **Branch `particles` — ASCII colour customisation** — ASCII shader gained `colorLow`/`colorHigh` float3 uniforms (Swift-side packed as SIMD4 for Metal alignment); single HUD `hue` slider feeds `applyAsciiHue` which builds two NSColors and packs as deviceRGB. (The companion thought-flash overlay shipped that day was rejected and removed; see the tracery entry above.) | 2026-05-13 |
| **Branch `particles` — ASCII-dither overlay + sonar shockwave ring** — new `RenderKit/Ascii.metal` (cell-quantised camera-through-mask → glyph atlas index by luminance) + `RenderKit/AsciiAtlas.swift` (CGContext+NSGraphicsContext bakes 10-glyph monospaced strip into R8 texture once at renderer init). Single audio-triggered ring (`shockSpeed=0.55 uv/s`, gaussian width `0.045`, peak boost `0.85`) expands from `asciiOrigin` (= bodyCenter) on `audio.latest.transient > 0.18`, rate-limited to 5.5 Hz. HUD: `ascii` toggle + `cell` slider (4–28 px) + manual `pulse` button. Drawn as last pass on both trail and no-trail draw paths so glyphs sit cleanly on top of swarm + boxes + camera. | 2026-05-13 |
| **Branch `particles` — shockwave breaks silhouette + negative-camera flash boxes** — added `bodyCenter` uniform (avg of hip joints from Vision); shockwave origin moved from screen-pan-offset to inside-body, kick magnitude 6→9, falloff softened so the impulse reaches the silhouette edge with force; particle_fragment `escapeBoost` opens the mask gate during transients so particles stay visible streaming past the body. New `RenderKit/NegativeBoxes.metal` + pipeline + persistent buffer + CPU `[NegativeBoxState]` + `flashNegativeBox(center:halfSize:duration:peak:)` API + triangular alpha envelope; ContentView Timer at ~9 Hz picks 1–3 random confident joints and flashes negative-inverted camera windows around them. HUD got `neg boxes` toggle + intensity slider. | 2026-05-13 |
| **Branch `particles` — motion trails + audio reactivity** — trail accumulator (`Trail.metal` + ping-pong bgra8 textures + decay-then-particles-then-present three-pass flow), `AudioKit/AudioReactor` (AVAudioEngine input tap → vDSP FFT N=1024 → low/mid/high/level/pan/transient EMA-smoothed snapshots, nonisolated worker thread, main-queue publish), 7 new audio uniforms in particle shader: bass swells curl flow, mid pushes time axis, high tints hue, level/transient drive glow, transient injects a stereo-panned outward shockwave with 1/(1+12d²) falloff. HUD gained trail slider+toggle, audio toggle/strength/gain sliders, 4-bar live meter (L/M/H/≡). Mic entitlement + Info.plist key wired. | 2026-05-13 |
| **Branch `particles` redesign — silhouette IS the swarm** — dark fixed background (renderer `darkBackground` flag skips camera blit, MTKView clears to black), curl-noise (divergence-free) fluid drift replacing value-noise jitter, mask-attractive force (gradient pulls particles INTO body) replacing repulsion, alpha gated by mask coverage in the fragment, faster life decay in empty space so density self-tracks the silhouette. New knobs (attraction/flow/swirl/damping/size/glow/hue/maskGate). | 2026-05-13 |
| **Branch `particles` scaffold** — `ParticleKit` module (`Particles.metal` compute+render, `ParticleField` Observable class), renderer wired to dispatch the compute pass and draw point-sprites additively over the camera blit, ContentView rewritten with particle HUD (enable / count picker / 5 sliders / mask-weight) and H-key HUD-toggle. Diffusion + PromptKit references removed from UI on this branch. Build green, 0 warnings. | 2026-05-12 |
| Bugfix — presets/prompts had no visible effect because `strength × stepCount = 0.55 × 2 ≈ 1` effective denoising step. Bumped defaults to `strength=0.78, steps=4` and rebalanced every preset's `suggestedStrength` to 0.75–0.82. Trades ~660 ms/pass for ~1.2 s/pass; prompt now actually steers the output. | 2026-05-12 |
| Mask mode tri-state — `Composite.metal` + `CameraMetalRenderer.MaskMode { full, person, background }` replacing the boolean `mask_enabled`. Default flipped to `.background` so prompts like "starry night" repaint the environment while the person stays as live camera. HUD checkbox replaced with 3-segment Picker. | 2026-05-12 |
| Bugfix — `DiffusionBenchmark.init()` was hardcoded to the 512 model bundle while `variant` defaulted to 384, so the first live pass exploded with `Encoder.Error 0`. Init now uses literal defaults that match the property declarations. | 2026-05-11 |
| **M4 — PromptKit + UI polish** — `StylePreset` library (8 presets, each with suggestedSteps + suggestedStrength), `PromptComposer` with deterministic pose modifier (3 rules) + motion modifier (EMA-smoothed centroid speed → 3 buckets), wired through `DiffusionBenchmark.effectivePrompt`, preset-chip picker, extra-prompt field, on-screen effective-prompt readout with hint chips, modifier toggles, fullscreen button. Build green, 0 warnings. | 2026-05-11 |
| **Diffusion-perf A/B winner picked** — 384×384 + CPU+ANE = ~660 ms / pass (~1.5 Hz) vs 512/ANE's 997 ms. Set as default in `DiffusionBenchmark.variant`. | 2026-05-11 |
| **M3.5 — Diffusion-perf A/B knobs in UI** — `ComputeUnitChoice` + `ModelVariant` enums, mutable pipeline with explicit rebuild on change, `LiveDiffusionDriver` reads settings live so no restart needed, segmented pickers in HUD | 2026-05-11 |
| **M3 — Live diffusion loop + temporal blend + person composite** — `Composite.metal`, extended renderer (aiPrev/aiNext/mask slots, EMA-tuned blend cycle), `LiveDiffusionDriver` continuous loop, Live toggle + style slider + mask checkbox in HUD, stall detection | 2026-05-11 |
| **M2 verified on hardware** — Vision pass measured at ~32 ms (~31 Hz capable; throttled at 15 Hz to leave budget for diffusion) | 2026-05-11 |
| **M2 — VisionKit shipped** — `VisionFrame`, `VisionProcessor` actor (`.balanced` seg + body pose), `VisionSession` polling driver @ 15 Hz, `PoseOverlay` SwiftUI canvas with skeleton + dots, HUD toggle | 2026-05-11 |
| **M1 verified on hardware** — SD Turbo img2img runs end-to-end, 997 ms / pass measured (~1 FPS, below the ≥3 FPS prediction). ProjectDocument §13 #1 updated with the real number. | 2026-05-11 |
| **M1 scaffold** — `DiffusionKit` actor (img2img, CFG=0, dpm-solver, reduceMemory), benchmark UI, model-folder reveal | 2026-05-11 |
| M1 first-run bugfix — `PixelBufferToCGImage` now produces guaranteed-exact 512×512 output via fixed-size `CGContext`, avoiding `Encoder.Error.sampleInputShapeNotCorrect` | 2026-05-11 |
| Added `apple/ml-stable-diffusion` SwiftPM dependency (1.1.1) via direct pbxproj edit       | 2026-05-11 |
| Continuity Camera reconnect bug fix — device-connected/disconnected notifications, auto-switch to iPhone, manual Reconnect + device picker in HUD | 2026-05-11 |
| **M0 verified on hardware** — Continuity Camera end-to-end latency <150 ms (target met) | 2026-05-11 |
| **M0 — Project skeleton + camera passthrough** (build green, 0 warnings)                  | 2026-05-11 |
| `CaptureKit/CameraCapture` — AVCaptureSession wrapper, AsyncStream<CVPixelBuffer>, latest-frame-wins, Continuity-Camera-preferred device picker | 2026-05-11 |
| `RenderKit/CameraMetalRenderer` + `Passthrough.metal` — MTKView delegate, CVMetalTextureCache, full-screen-triangle blit, FPS log | 2026-05-11 |
| `RenderKit/CameraMetalView` — NSViewRepresentable wrapper                                  | 2026-05-11 |
| `AppShell/CameraSession` + rewritten `ContentView` with status HUD                         | 2026-05-11 |
| Camera entitlement + `NSCameraUsageDescription` Info.plist key                             | 2026-05-11 |
| Constraint-driven rewrite of `ProjectDocument.md`                                          | 2026-05-11 |
| Established `Journal.md` and `Roadmap.md`                                                  | 2026-05-11 |
| Locked v1 stack: Continuity Camera + Vision + SD Turbo + Metal                             | 2026-05-11 |

---

## Blocked

_None yet._ Use this section for items waiting on an external decision, hardware delivery, license clarification, etc. Each entry must record: **what** is blocked, **on what** it's blocked, **since when**, and **the unblock criterion**.

---

## Upcoming Milestones

Next 4 weeks (rough order, not committed dates):

1. **M0 — Project skeleton + camera passthrough.** Goal: prove the boring path works end-to-end before touching ML.
2. **M1 — Diffusion benchmark.** Goal: convert the central performance assumption into a measured number. **This is the highest-risk milestone.** If it fails, we change the model, the resolution, or the strategy *before* writing more code.
3. **M2 — Vision integration.** Goal: parallel track to M1; the Vision pipeline is well-understood and low-risk.
4. **M3 — Live loop + temporal blend.** Goal: the first build that actually feels like Artlify.

---

## Post-v1 / Conditional

Items deliberately out of scope for v1. Each lists the **trigger condition** that would justify revisiting.

| Item                                       | Trigger to reconsider                                                          |
| ------------------------------------------ | ------------------------------------------------------------------------------ |
| ControlNet (pose-conditioned generation)   | v1 ships and users report the silhouette tracking isn't tight enough.          |
| StreamDiffusion-style pipeline             | A maintained Swift/CoreML port appears, or we hire ML capacity.                |
| Cloud diffusion fallback                   | We need to support sub-M-series Macs.                                          |
| Custom iOS streaming app                   | We need to support non-Apple-ecosystem cameras or longer-distance setups.      |
| Multi-person identity tracking             | A specific installation requires per-person prompts.                           |
| Audio reactivity                           | Concrete artist/installation request with a defined audio source.              |
| Projection mapping / multi-display         | Concrete venue requirement.                                                    |
| Recording / export pipeline                | Users ask for it more than once.                                               |
| LoRA / fine-tuning                         | We acquire a labeled dataset and a training machine.                           |

---

## Update protocol

- When you start an item, move it to **In Progress** and set status.
- When you finish, move it to **Completed** with a date and add a `Journal.md` entry if the work changed any decision in `ProjectDocument.md`.
- When blocked, move to **Blocked** with all four required fields filled in.
- Re-estimate effort if reality diverges by >2×; note the divergence in `Journal.md`.
